meta {
  name: List events
  type: http
  seq: 2
}

get {
  url: {{base_url}}/events
  body: none
  auth: bearer
}

auth:bearer {
  token: {{calendar_token}}
}

docs {
  Lists the calendar's events, sorted by dtstart. The recovery path
  for a lost uid or a duplicate from a retried create.
}
